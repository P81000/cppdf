#pragma once

#include <cstddef>

namespace cppdf {
    constexpr float DEFAULT_DPI = 200.0f;
    constexpr size_t SCROLL_STEP = 100uz;
    constexpr float INITIAL_ZOOM = 1.0f;
    constexpr float ZOOM_STEP = 0.1f;
    constexpr float MIN_ZOOM = 0.1f;
    constexpr float MAX_ZOOM = 1.0f;
}
